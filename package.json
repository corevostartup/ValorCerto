{
  "name": "valorcerto",
  "version": "0.1.0",
  "private": true,
  "description": "Backend serverless MVP do app ValorCerto",
  "scripts": {
    "build": "rm -rf web/dist && mkdir -p web/dist && cp web/index.html web/dist/ && cp -r web/privacy web/terms web/dist/",
    "dev": "netlify dev",
    "lint": "echo \"Sem lint configurado\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.8.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.0"
  }
}

